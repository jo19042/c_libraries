#ifndef GENERAL_FUNCTIONS_H
#define GENERAL_FUNCTIONS_H

/**
 * @brief Prints an array.
 * 
 * @param buf array to be printed
 * @param buf_size number of values in the array
 */
void array_print_10_at_a_time(int * buf, int buf_size);

#endif